# VirtualBankingSystem

Python and Streamlit virtual banking simulation.

See [run instructions](outputs/README.md) and the [assignment checklist](outputs/ASSIGNMENT-CHECKLIST.md).

```sh
python -m pip install -r outputs/requirements.txt
python -m streamlit run outputs/app.py
```
